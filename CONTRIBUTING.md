# Contributing

Small, focused pull requests are welcome. Build on Windows with the .NET 10 SDK and run `./scripts/build.ps1 -Check` before submitting changes. Describe the change and how you tested it.

Keep the UI native, copy concise, and transcription local. Do not commit recordings, model weights, credentials, or build output. Screenshots should use fictional demo data.
